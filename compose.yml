services:
  cli:
    build: .
    volumes:
      - ./databases:/app/databases:rw
    stdin_open: true
    tty: true
  server:
    build: .
    volumes:
      - ./databases:/app/databases:rw
    command: poetry run python src/server.py
